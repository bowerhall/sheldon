# Sheldon Configuration
#
# RECOMMENDED: Use Doppler for secrets management instead of this file.
# See docs/deployment.md for setup instructions.
#
# For local development: cp .env.example .env && fill in values

# =============================================================================
# REQUIRED
# =============================================================================

TELEGRAM_TOKEN=your_telegram_bot_token

# LLM Provider (need at least one)
# For Claude:
ANTHROPIC_API_KEY=your_anthropic_api_key
# For Kimi (cheaper alternative):
# KIMI_API_KEY=your_kimi_api_key

# =============================================================================
# DOMAIN & ROUTING
# =============================================================================

# Your domain (apps deploy to: appname.yourdomain.com)
DOMAIN=sheldon.example.com

# Let's Encrypt email for HTTPS (optional)
ACME_EMAIL=you@example.com

# =============================================================================
# OPTIONAL: Coder (AI code generation)
# =============================================================================

CODER_ENABLED=false
# Run in isolated containers (recommended for security)
CODER_ISOLATED=false
CODER_IMAGE=sheldon-coder-sandbox:latest
CODER_MODEL=kimi-k2.5

# Coder uses these keys (separate from main LLM)
NVIDIA_API_KEY=
KIMI_API_KEY=

# Git integration (for coder to push code)
GIT_TOKEN=
GIT_USER_NAME=Sheldon
GIT_USER_EMAIL=sheldon@example.com
GIT_ORG_URL=https://github.com/your-org

# =============================================================================
# OPTIONAL: Embeddings (semantic memory search)
# =============================================================================

# Options: ollama, voyage
EMBEDDER_PROVIDER=
EMBEDDER_BASE_URL=http://localhost:11434
EMBEDDER_MODEL=nomic-embed-text

# =============================================================================
# OPTIONAL: Heartbeat (proactive check-ins)
# =============================================================================

HEARTBEAT_ENABLED=false
HEARTBEAT_CHAT_ID=
HEARTBEAT_INTERVAL=8

# =============================================================================
# INTERNAL (usually don't need to change)
# =============================================================================

DEPLOYER_APPS_FILE=/apps/apps.yml
DEPLOYER_NETWORK=sheldon-net
TZ=UTC
