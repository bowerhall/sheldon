# Coder Sandbox - Ephemeral code execution container
#
# This container is spawned on-demand by Sheldon when CODER_ISOLATED=true
# Code is generated by Sheldon via API, then executed here safely.

FROM ubuntu:22.04

ENV DEBIAN_FRONTEND=noninteractive

# Install dependencies for code execution
RUN apt-get update && apt-get install -y \
    curl \
    git \
    ca-certificates \
    build-essential \
    python3 \
    python3-pip \
    nodejs \
    npm \
    && rm -rf /var/lib/apt/lists/*

# Create non-root user for security
RUN useradd -m -s /bin/bash coder && \
    mkdir -p /workspace && \
    chown -R coder:coder /workspace

USER coder
WORKDIR /workspace

CMD ["/bin/bash"]
