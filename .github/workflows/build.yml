name: Build and Push Images

on:
  push:
    branches: [main]
    paths:
      - 'core/**'
      - 'pkg/**'
      - '.github/workflows/build.yml'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build-sheldon:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push sheldon
        uses: docker/build-push-action@v5
        with:
          context: .
          file: core/Dockerfile
          push: true
          provenance: false
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/sheldon:latest
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/sheldon:${{ github.sha }}

  build-browser-sandbox:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push browser-sandbox
        uses: docker/build-push-action@v5
        with:
          context: core/deploy/browser-sandbox
          push: true
          provenance: false
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/sheldon-browser-sandbox:latest
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/sheldon-browser-sandbox:${{ github.sha }}

  build-coder-sandbox:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push coder-sandbox
        uses: docker/build-push-action@v5
        with:
          context: core/deploy/coder-sandbox
          push: true
          provenance: false
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/sheldon-coder-sandbox:latest
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/sheldon-coder-sandbox:${{ github.sha }}

  deploy:
    needs: [build-sheldon, build-browser-sandbox, build-coder-sandbox]
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /opt/sheldon
            docker compose pull
            docker compose up -d
