apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - kora-apps-namespace.yaml
  - rbac.yaml
  - network-policy.yaml
  - kora-apps-network-policy.yaml
  - minio.yaml
  # - backup.yaml  # optional, depends on minio
  - coder-artifacts.yaml  # needed for k8s Jobs mode
  - kora.yaml
  - config.yaml
  - essence.yaml
  - secrets.yaml
  # skills.yaml removed - using git repo instead (CODER_SKILLS_REPO)
