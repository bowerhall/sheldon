apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sheldon-memory
  namespace: sheldon
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sheldon
  namespace: sheldon
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sheldon
  template:
    metadata:
      labels:
        app: sheldon
    spec:
      serviceAccountName: sheldon
      securityContext:
        fsGroup: 1000
      initContainers:
        - name: fix-permissions
          image: alpine:3.19
          command: ["sh", "-c", "chown -R 1000:1000 /data && chmod 666 /var/run/docker.sock"]
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: memory
              mountPath: /data
            - name: docker-socket
              mountPath: /var/run/docker.sock
        - name: clone-skills
          image: alpine/git:latest
          command:
            - sh
            - -c
            - |
              if [ -n "$CODER_SKILLS_REPO" ]; then
                git clone --depth 1 "$CODER_SKILLS_REPO" /skills
              else
                echo "no skills repo configured, skipping"
                mkdir -p /skills
              fi
          envFrom:
            - configMapRef:
                name: sheldon-config
          volumeMounts:
            - name: skills
              mountPath: /skills
      containers:
        - name: sheldon
          image: ghcr.io/bowerhall/sheldon:latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: sheldon-config
            - secretRef:
                name: sheldon-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          volumeMounts:
            - name: memory
              mountPath: /data
            - name: essence
              mountPath: /essence
              readOnly: true
            - name: docker-socket
              mountPath: /var/run/docker.sock
            - name: sandbox
              mountPath: /tmp/sheldon-sandbox
            - name: coder-artifacts
              mountPath: /tmp/sheldon-artifacts
            - name: skills
              mountPath: /skills
              readOnly: true
      volumes:
        - name: memory
          persistentVolumeClaim:
            claimName: sheldon-memory
        - name: essence
          configMap:
            name: sheldon-essence
        - name: docker-socket
          hostPath:
            path: /var/run/docker.sock
            type: Socket
        - name: sandbox
          emptyDir: {}
        - name: coder-artifacts
          persistentVolumeClaim:
            claimName: sheldon-coder-artifacts
        - name: skills
          emptyDir: {}
