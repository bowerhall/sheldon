# pvc for ephemeral claude code job artifacts
# jobs write output here, sheldon reads for build/deploy
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sheldon-coder-artifacts
  namespace: sheldon
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
