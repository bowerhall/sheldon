# minimal config - uses external apis only (no local models)
apiVersion: v1
kind: ConfigMap
metadata:
  name: sheldon-config
  namespace: sheldon
data:
  # no local embedder - use external api or disable
  EMBEDDER_PROVIDER: ""
  EMBEDDER_BASE_URL: ""
  EMBEDDER_MODEL: ""
