apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kora

resources:
  - namespace.yaml
  - ollama.yaml
  - kora.yaml
  - config.yaml
  - essence.yaml
  # Create secrets.yaml from secrets.yaml.example (gitignored)
  # - secrets.yaml
